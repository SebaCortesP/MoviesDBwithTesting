# # H2 Config (asegúrate que esté en src/test/resources/)
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=Oracle;INIT=CREATE SCHEMA IF NOT EXISTS TESTDB
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# # Forzar creación de tablas
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.generate-ddl=true
# spring.jpa.show-sql=true

# # Configuración específica para Oracle compatibility
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.properties.hibernate.default_schema=TESTDB
# spring.jpa.properties.hibernate.hbm2ddl.auto=create-drop

# # Deshabilitar Flyway/Liquibase en tests
# spring.flyway.enabled=false
# spring.liquibase.enabled=false